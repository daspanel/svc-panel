#!/bin/sh

set -x

# Install Python 3.6
apk add --no-cache --update libssl1.0 libcurl libxslt python3
python3 -m ensurepip
rm -r /usr/lib/python*/ensurepip

# Install pip
curl "https://bootstrap.pypa.io/get-pip.py" | python3
pip install --no-cache-dir --upgrade pip setuptools
rm -r /root/.cache

