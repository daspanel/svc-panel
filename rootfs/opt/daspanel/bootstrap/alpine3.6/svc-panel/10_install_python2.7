#!/bin/sh

set -x

# Install Python 2.7
apk add --no-cache --update libssl1.0 libcurl libxslt python2
python -m ensurepip
rm -r /usr/lib/python*/ensurepip

# Install pip
curl "https://bootstrap.pypa.io/get-pip.py" | python
pip install --no-cache-dir --upgrade pip setuptools
rm -r /root/.cache

